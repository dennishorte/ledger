/**
 * Shared types for the UI app.
 *
 * Per 01-ui/01-shell.md (decision D5): the shell node intentionally declares
 * zero domain types. Domain contracts (Document, Task, Lifecycle, ...) will be
 * defined alongside each per-panel node as data fetching is introduced.
 */

export {};
