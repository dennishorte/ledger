{
  "name": "ledger-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.28.0",
  "dependencies": {
    "@ledger/server": "workspace:*"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "kill-all": "pids=$(lsof -iTCP:4179,4180 -sTCP:LISTEN -t); if [ -z \"$pids\" ]; then echo 'no app/server processes running on :4179 or :4180'; else echo \"killing $pids\" && kill $pids; fi"
  }
}
